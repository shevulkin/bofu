# Код застосунку — назовні не віддається нічого. Працює і без mod_rewrite.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
