<div class="admin-head">
  <h1 class="h-serif">Розсилка</h1>
  <a class="btn btn-line btn-sm" href="<?= e(url('/admin/subscribers')) ?>?export=csv"
     data-help-title="Експорт CSV"
     data-help="Вивантажує список підписників файлом, який відкривається в Excel і завантажується в сервіс розсилок.

У файлі є персональне посилання на відписку для кожної адреси. Обовʼязково вставляйте його в лист: без нього розсилка порушує правила і швидко потрапляє в спам.

Вивантажений файл містить особисті дані людей — не пересилайте його стороннім і не лишайте в спільних папках.">⬇ Експорт CSV</a>
</div>
<div class="admin-card">
  <p class="dim" style="margin:0">Активних підписників: <b style="color:var(--gold)"><?= (int)$active_count ?></b>.
    Адреси потрапляють сюди лише тоді, коли покупець сам поставив галку згоди — при оформленні замовлення або в профілі.
    У кожен лист розсилки обовʼязково вставляйте персональне посилання на відписку (є у CSV).</p>
</div>
<table class="tbl">
  <tr><th>Email</th><th>Ім'я</th>
    <th data-help-title="Колонка «Звідки»"
        data-help="Де саме людина дала згоду на розсилку.

«Оформлення» — поставила галку в формі замовлення. «Профіль» — увімкнула підписку у своєму кабінеті.

Адреса не може потрапити сюди інакше: без явної галки ніхто в список не додається. Саме тому цей список і можна законно використовувати.">Звідки</th>
    <th data-help-title="Колонка «Дата згоди»"
        data-help="Коли людина погодилась отримувати листи.

Це доказ згоди — зберігайте його. Якщо хтось поскаржиться на небажану розсилку, саме ця дата й джерело підтверджують, що підписка була добровільною.">Дата згоди</th>
    <th data-help-title="Колонка «Стан»"
        data-help="«Підписаний» — листи надсилати можна.

«Відписаний» із датою — людина відмовилась. Надсилати їй розсилку не можна, навіть якщо адреса лишилась у списку. Запис зберігається саме для того, щоб випадково не додати її знову.">Стан</th>
    <th></th></tr>
  <?php foreach ($rows as $r): ?>
    <tr>
      <td><b><?= e($r['email']) ?></b></td>
      <td><?= e($r['name'] ?: '—') ?></td>
      <td class="dim"><?= $r['source'] === 'profile' ? 'Профіль' : 'Оформлення' ?></td>
      <td class="dim"><?= e($r['created_at']) ?></td>
      <td>
        <?= (int)$r['active'] === 1
          ? '<span class="status-pill st-processing">Підписаний</span>'
          : '<span class="status-pill st-canceled">Відписаний ' . e((string)$r['unsubscribed_at']) . '</span>' ?>
      </td>
      <td style="white-space:nowrap">
        <?php if ((int)$r['active'] === 1): ?>
          <form method="post" action="<?= e(url('/admin/subscribers')) ?>" style="display:inline"><?= Csrf::field() ?>
            <input type="hidden" name="_action" value="unsubscribe"><input type="hidden" name="id" value="<?= (int)$r['id'] ?>">
            <button class="btn btn-line btn-xs" data-help-title="Кнопка «Відписати»"
                    data-help="Позначає, що людина більше не отримує розсилку.

Тисніть, коли вона попросила про це особисто — у листі, по телефону чи в месенджері.

Запис при цьому лишається зі станом «Відписаний» і датою. Це навмисно: так видно, що згоду відкликано, і адресу не додадуть у список повторно.">Відписати</button>
          </form>
        <?php endif; ?>
        <form method="post" action="<?= e(url('/admin/subscribers')) ?>" style="display:inline"><?= Csrf::field() ?>
          <input type="hidden" name="_action" value="delete"><input type="hidden" name="id" value="<?= (int)$r['id'] ?>">
          <button class="btn btn-danger btn-xs" onclick="return confirm('Видалити запис назавжди?')"
                  data-help-title="Кнопка «Видалити запис»"
                  data-help="Стирає запис назавжди, разом із датою згоди й фактом відписки.

Для звичайної відмови від розсилки беріть «Відписати» — воно зберігає слід. Видалення потрібне лише тоді, коли людина вимагає повністю прибрати свої дані.

Обережно: після видалення ця сама адреса може знову потрапити в список при наступному замовленні з галкою, і доказу попередньої відписки вже не буде.">✕</button>
        </form>
      </td>
    </tr>
  <?php endforeach; ?>
  <?php if (!$rows): ?><tr><td colspan="6" class="dim">Поки що ніхто не підписався.</td></tr><?php endif; ?>
</table>
