<div class="admin-head"><h1 class="h-serif">Очікують товар</h1></div>

<p class="card-lead" style="margin:-14px 0 22px">
  Люди, які просили повідомити про наявність. Це не побажання, а <b>черга на виробництво</b>:
  згори те, чого чекає найбільше людей. Щойно поставите залишок більший за нуль,
  усім із рядка піде повідомлення, і рядок зникне сам.
</p>

<?php if (!$rows): ?>
  <div class="admin-card dim">Поки що ніхто нічого не чекає.</div>
<?php else: ?>
<table class="tbl">
  <tr>
    <th data-help-title="Колонка «Позиція»"
        data-help="Товар і варіант, якого чекають. Якщо варіанта немає — чекають товар загалом.

Назва клікабельна: веде в картку товару, де й ставлять залишок.">Позиція</th>
    <th class="num" data-help-title="Колонка «Чекають»"
        data-help="Скільки різних людей просили повідомити саме про цю позицію.

Одна людина рахується один раз, скільки б вона не тиснула кнопку.

Це найчесніший сигнал попиту, який є в системі: люди вже хотіли купити, але не змогли. Список відсортований саме за цим числом — згори те, що вигідніше виробити першим.">Чекають</th>
    <th data-help-title="Колонка «Чекають від»"
        data-help="Дата найдавнішого запиту на цю позицію.

Разом із кількістю дає повну картину: троє чекають місяць — це гірше, ніж троє чекають з учора.">Чекають від</th>
    <th class="num" data-help-title="Колонка «Зараз на складі»"
        data-help="Скільки цієї позиції зараз у мережі загалом.

Тут майже завжди нуль — на те й черга. Але не обовʼязково: товар могли щойно завезти в одну точку, а сповіщення підуть при наступному збереженні залишків.

Якщо тут не нуль, а рядок висить — просто відкрийте картку товару й збережіть залишки: це закриє чергу.">Зараз на складі</th>
    <th></th>
  </tr>
  <?php foreach ($rows as $r): ?>
    <tr>
      <td>
        <b><?= e($r['product_name']) ?></b><?= $r['variant_name'] ? ' · ' . e($r['variant_name']) : '' ?>
      </td>
      <td class="num"><b style="color:var(--gold)"><?= (int)$r['cnt'] ?></b></td>
      <td class="dim"><?= e(date('d.m.Y', strtotime((string)$r['since']))) ?></td>
      <td class="num<?= (int)$r['stock'] > 0 ? '' : ' dim' ?>"><?= (int)$r['stock'] ?></td>
      <td><a class="btn btn-line btn-xs" href="<?= e(url('/admin/products/' . (int)$r['product_id'])) ?>">Поставити залишок</a></td>
    </tr>
  <?php endforeach; ?>
</table>
<?php endif; ?>
