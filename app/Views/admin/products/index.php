<div class="admin-head">
  <h1 class="h-serif">Товари</h1>
  <div style="display:flex;gap:10px">
    <a class="btn btn-line btn-sm" href="<?= e(url('/admin/products/bulk')) ?>"
       data-help-title="Масове редагування"
       data-help="Таблиця, де можна швидко поправити ціни й залишки одразу багатьом товарам, не заходячи в кожен.

Беріть її, коли треба зробити одну дію багато разів: переоцінка після постачання, перерахунок складу, зміна сортування.

Для одного товару зручніша кнопка «Редагувати» в рядку — там доступні фото, опис, варіанти й характеристики, яких у масовому режимі немає.">Масове редагування</a>
    <a class="btn btn-gold btn-sm" href="<?= e(url('/admin/products/new')) ?>"
       data-help-title="Новий товар"
       data-help="Створює порожню картку товару.

Мінімум, щоб товар зʼявився на сайті: назва, категорія, ціна й позначка «Активний». Фото, опис, варіанти й залишки можна дозаповнити потім.

Товар без позначки «Активний» на сайті не показується — це зручно, коли картку заводять заздалегідь.">+ Новий товар</a>
  </div>
</div>
<form class="admin-card" method="get" action="<?= e(url('/admin/products')) ?>" style="display:flex;gap:14px;align-items:end;flex-wrap:wrap">
  <div style="flex:1;min-width:200px" data-help-title="Пошук"
       data-help="Шукає за назвою товару й за артикулом (SKU) одночасно.

Досить частини слова: «лип» знайде «Мед липовий». Регістр не має значення.

Артикул зручніший, коли назви схожі: клієнт диктує код з етикетки, ви одразу знаходите потрібне.

Порожнє поле — показати всі товари.">
    <label>Пошук</label><input type="text" name="q" value="<?= e($q) ?>" placeholder="Назва або артикул"></div>
  <div data-help-title="Категорія"
       data-help="Показати лише товари однієї категорії. «Всі» — без фільтра.

Працює разом із пошуком: можна шукати «мед» усередині категорії «Подарункові набори».

Це фільтр лише для цього списку — на сайт і на самі товари він не впливає.">
    <label>Категорія</label>
    <select name="cat">
      <option value="">Всі</option>
      <?php foreach ($categories as $c): ?>
        <option value="<?= (int)$c['id'] ?>" <?= $cat == $c['id'] ? 'selected' : '' ?>><?= e($c['name']) ?></option>
      <?php endforeach; ?>
    </select>
  </div>
  <button class="btn btn-gold btn-sm" type="submit" data-help-title="Кнопка «Знайти»"
          data-help="Застосовує пошук і фільтр категорії до списку нижче.

Нічого не змінює в товарах — лише показує потрібні. Щоб повернути повний список, очистіть поле пошуку, поставте категорію «Всі» й натисніть ще раз.">Знайти</button>
  <?php /* Прийшли з довідника брендів — фільтр треба і зберегти при пошуку,
           і показати: інакше незрозуміло, чому список короткий. */ ?>
  <?php if ($brand): ?>
    <input type="hidden" name="brand" value="<?= (int)$brand['id'] ?>">
    <span class="dim" style="margin-bottom:10px">Бренд: <b><?= e($brand['name']) ?></b>
      <a href="<?= e(url('/admin/products')) ?>" title="Прибрати фільтр">✕</a></span>
  <?php endif; ?>
</form>
<table class="tbl">
  <tr><th></th>
    <th data-help-title="Колонка «Назва»"
        data-help="Назва товару так, як її бачить покупець на сайті.

Сірим під нею — артикул (SKU), якщо він заданий. Це ваш внутрішній код для обліку; покупцю він не показується в каталозі.

Ліворуч — головне фото картки. Якщо там заглушка з банкою меду, значить фото товару ще не додали.">Назва</th>
    <th data-help-title="Колонка «Категорія»"
        data-help="До якої категорії належить товар — саме там покупець знайде його в каталозі.

Прочерк означає, що категорію не призначено. Такий товар важче знайти на сайті: він не потрапляє в жоден розділ каталогу, лише в пошук.

Категорія також визначає, чи спрацює на товар акція, задана на категорію.">Категорія</th>
    <th data-help-title="Колонка «Ціна»"
        data-help="Базова ціна товару — та, що діє за замовчуванням.

Це ще не обовʼязково те, що бачить покупець: ціну може перебивати окрема ціна магазину, ціна варіанта або активна акція.

«За запитом» означає, що ціну не задано взагалі — покупець побачить не число, а пропозицію зв'язатися.">Ціна</th>
    <th data-help-title="Колонка «Наявність»"
        data-help="Скільки одиниць товару зараз на складі всієї мережі, а нижче — розкладка по магазинах.

«З варіантів» означає, що залишок рахується не по товару загалом, а по його варіантах (обʼєм, вага, колір) і тут показана їх сума.

Нуль не забороняє покупку: сайт дозволяє замовляти понад залишок, і замовлення все одно прийдеться виконувати. Тому тримайте цифри чесними.">Наявність</th>
    <th data-help-title="Колонка «Статус»"
        data-help="«Активний» — товар показується на сайті й доступний до замовлення.

«Прихований» — товар є в базі, але на сайті його немає: ні в каталозі, ні в пошуку. Зручно для сезонних позицій і того, що тимчасово не продаєте. Дані при цьому не втрачаються.

«Хіт» — товар піднятий угору каталогу й позначений як рекомендований. Ставте небагатьом, інакше позначка перестає щось означати.">Статус</th>
    <th></th></tr>
  <?php foreach ($products as $p): $pid = (int)$p['id']; ?>
    <tr>
      <td style="width:64px">
        <?php $ph = Catalog::photo($p); ?>
        <img class="img-thumb" src="<?= e(asset($ph)) ?>" alt="" loading="lazy">
      </td>
      <td><b><?= e($p['name']) ?></b><?php if ($p['sku']): ?><div class="dim"><?= e($p['sku']) ?></div><?php endif; ?></td>
      <td class="muted"><?= e($p['cat_name'] ?? '—') ?></td>
      <td><?= e(price_fmt($p['base_price'])) ?></td>
      <td>
        <?php $byStore = $stocks[$pid] ?? []; $total = array_sum($byStore); ?>
        <b><?= $total ?></b> шт
        <div class="dim">
          <?php $bits = [];
            foreach ($stores as $s) $bits[] = e($s['city'] ?: $s['name']) . ': ' . (int)($byStore[(int)$s['id']] ?? 0);
            echo implode(' · ', $bits);
            if (!empty($variant_count[$pid])) echo '<br>з варіантів: ' . (int)$variant_count[$pid]; ?>
        </div>
      </td>
      <td><?= $p['active'] ? '<span class="status-pill st-processing">Активний</span>' : '<span class="status-pill st-canceled">Прихований</span>' ?>
          <?= $p['featured'] ? ' <span class="status-pill st-new">Хіт</span>' : '' ?></td>
      <td><a class="btn btn-line btn-xs" href="<?= e(url('/admin/products/' . $p['id'])) ?>">Редагувати</a></td>
    </tr>
  <?php endforeach; ?>
</table>
